# Iron Bridge Mobility Solutions

## Code style

- Keep comments in the code as low as reasonably possible. Well-named
  variables, functions, and components should make the code self-explanatory.
  Only comment when something is genuinely non-obvious (a workaround, a
  subtle constraint, a reason a simpler approach won't work).
